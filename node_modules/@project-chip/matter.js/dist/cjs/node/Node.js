"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __knownSymbol = (name, symbol) => (symbol = Symbol[name]) ? symbol : Symbol.for("Symbol." + name);
var __typeError = (msg) => {
  throw TypeError(msg);
};
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var __using = (stack, value, async) => {
  if (value != null) {
    if (typeof value !== "object" && typeof value !== "function") __typeError("Object expected");
    var dispose, inner;
    if (async) dispose = value[__knownSymbol("asyncDispose")];
    if (dispose === void 0) {
      dispose = value[__knownSymbol("dispose")];
      if (async) inner = dispose;
    }
    if (typeof dispose !== "function") __typeError("Object not disposable");
    if (inner) dispose = function() {
      try {
        inner.call(this);
      } catch (e) {
        return Promise.reject(e);
      }
    };
    stack.push([async, dispose, value]);
  } else if (async) {
    stack.push([async]);
  }
  return value;
};
var __callDispose = (stack, error, hasError) => {
  var E = typeof SuppressedError === "function" ? SuppressedError : function(e, s, m, _) {
    return _ = Error(m), _.name = "SuppressedError", _.error = e, _.suppressed = s, _;
  };
  var fail = (e) => error = hasError ? new E(e, error, "An error was suppressed during disposal") : (hasError = true, e);
  var next = (it) => {
    while (it = stack.pop()) {
      try {
        var result = it[1] && it[1].call(it[2]);
        if (it[0]) return Promise.resolve(result).then(next, (e) => (fail(e), next()));
      } catch (e) {
        fail(e);
      }
    }
    if (hasError) throw error;
  };
  return next();
};
var Node_exports = {};
__export(Node_exports, {
  Node: () => Node
});
module.exports = __toCommonJS(Node_exports);
var import_NodeActivity = require("../behavior/context/NodeActivity.js");
var import_MatterError = require("../common/MatterError.js");
var import_Endpoint = require("../endpoint/Endpoint.js");
var import_Environment = require("../environment/Environment.js");
var import_RuntimeService = require("../environment/RuntimeService.js");
var import_Diagnostic = require("../log/Diagnostic.js");
var import_DiagnosticSource = require("../log/DiagnosticSource.js");
var import_Logger = require("../log/Logger.js");
var import_Construction = require("../util/Construction.js");
var import_NodeLifecycle = require("./NodeLifecycle.js");
/**
 * @license
 * Copyright 2022-2024 Matter.js Authors
 * SPDX-License-Identifier: Apache-2.0
 */
const logger = import_Logger.Logger.get("Node");
class Node extends import_Endpoint.Endpoint {
  #environment;
  #runtime;
  constructor(config) {
    const parentEnvironment = config.environment ?? import_Environment.Environment.default;
    if (config.id === void 0) {
      config.id = `node${parentEnvironment.vars.increment("node.nextFallbackId")}`;
    }
    super(config);
    this.#environment = new import_Environment.Environment(config.id, parentEnvironment);
    this.#environment.set(import_NodeActivity.NodeActivity, new import_NodeActivity.NodeActivity());
    if (this.lifecycle.hasNumber) {
      if (this.number !== 0) {
        throw new import_MatterError.ImplementationError("The root endpoint ID must be 0");
      }
    } else {
      this.number = 0;
    }
    this.construction.start();
    this.lifecycle.online.on(() => {
      this.statusUpdate("is online");
    });
    this.lifecycle.offline.on(() => {
      this.statusUpdate("is offline");
    });
  }
  get env() {
    return this.#environment;
  }
  /**
   * Lifecycle information.
   */
  createLifecycle() {
    return new import_NodeLifecycle.NodeLifecycle(this);
  }
  /**
   * Bring the node online.
   */
  async start() {
    this.env.runtime.add(this);
    try {
      await this.construction.ready;
      if (this.#runtime) {
        return;
      }
      this.statusUpdate("going online");
      this.#runtime = this.createRuntime();
      this.#runtime.construction.start();
      await this.#runtime.construction.ready;
    } catch (e) {
      this.env.runtime.delete(this);
      throw e;
    }
  }
  /**
   * @deprecated use {@link start}
   */
  async bringOnline() {
    return this.start();
  }
  /**
   * Run the node in standalone mode.  Returns when the node is closed.
   */
  async run() {
    await this.start();
    await this.construction.closed;
  }
  /**
   * Take the node offline but leave state and structure intact.  Happens automatically on close.
   *
   * Once the node is offline you may use {@link start} to bring the node online again.
   */
  async cancel() {
    if (!this.#runtime) {
      return;
    }
    this.statusUpdate("going offline");
    await this.#runtime?.close();
    this.#runtime = void 0;
  }
  async close() {
    if (this.#runtime) {
      await this.cancel();
    }
    await super.close();
  }
  get [import_RuntimeService.RuntimeService.label]() {
    return ["Runtime for", import_Diagnostic.Diagnostic.strong(this.toString())];
  }
  get [import_Diagnostic.Diagnostic.value]() {
    var _stack = [];
    try {
      const nodeActivity = this.#environment.get(import_NodeActivity.NodeActivity);
      const _activity = __using(_stack, nodeActivity.begin("diagnostics"));
      return import_Diagnostic.Diagnostic.node("\u{1F9E9}", this.id, {
        children: [
          import_Diagnostic.Diagnostic.strong("Structure"),
          import_Diagnostic.Diagnostic.list([super[import_Diagnostic.Diagnostic.value]]),
          import_Diagnostic.Diagnostic.strong("Activity"),
          nodeActivity[import_Diagnostic.Diagnostic.value]
        ]
      });
    } catch (_) {
      var _error = _, _hasError = true;
    } finally {
      __callDispose(_stack, _error, _hasError);
    }
  }
  get lifecycle() {
    return super.lifecycle;
  }
  statusUpdate(message) {
    logger.notice(import_Diagnostic.Diagnostic.strong(this.toString()), message);
  }
  async [import_Construction.Construction.destruct]() {
    await this.cancel();
    await super[import_Construction.Construction.destruct]();
    import_DiagnosticSource.DiagnosticSource.delete(this);
  }
  /**
   * Normal endpoints must have an owner to complete construction but Nodes have no such precondition for
   * construction.
   */
  assertConstructable() {
  }
}
((Node2) => {
  function nodeConfigFor(defaultType, configuration, options) {
    if (!options) {
      options = {};
    }
    if (configuration === void 0) {
      return {
        type: defaultType,
        ...options
      };
    }
    if (configuration.deviceType !== void 0) {
      return {
        type: configuration,
        ...options
      };
    }
    return {
      type: defaultType,
      ...configuration
    };
  }
  Node2.nodeConfigFor = nodeConfigFor;
})(Node || (Node = {}));
//# sourceMappingURL=Node.js.map
