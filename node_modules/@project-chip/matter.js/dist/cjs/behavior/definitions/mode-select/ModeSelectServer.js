"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var ModeSelectServer_exports = {};
__export(ModeSelectServer_exports, {
  ModeSelectServer: () => ModeSelectServer,
  ModeSelectServerLogic: () => ModeSelectServerLogic
});
module.exports = __toCommonJS(ModeSelectServer_exports);
var import_ClusterType = require("../../../cluster/ClusterType.js");
var import_GeneralDiagnosticsCluster = require("../../../cluster/definitions/GeneralDiagnosticsCluster.js");
var import_ModeSelectCluster = require("../../../cluster/definitions/ModeSelectCluster.js");
var import_OnOffCluster = require("../../../cluster/definitions/OnOffCluster.js");
var import_RootEndpoint = require("../../../endpoint/definitions/system/RootEndpoint.js");
var import_Logger = require("../../../log/Logger.js");
var import_StatusCode = require("../../../protocol/interaction/StatusCode.js");
var import_GeneralDiagnosticsBehavior = require("../general-diagnostics/GeneralDiagnosticsBehavior.js");
var import_OnOffServer = require("../on-off/OnOffServer.js");
var import_ModeSelectBehavior = require("./ModeSelectBehavior.js");
/**
 * @license
 * Copyright 2022-2024 Matter.js Authors
 * SPDX-License-Identifier: Apache-2.0
 */
const logger = import_Logger.Logger.get("ModeSelectServer");
const ModeSelectServerBase = import_ModeSelectBehavior.ModeSelectBehavior.with(import_ModeSelectCluster.ModeSelect.Feature.OnOff);
class ModeSelectServerLogic extends ModeSelectServerBase {
  initialize() {
    this.#assertCurrentMode();
    this.#assertStartUpMode();
    this.#assertOnMode();
    let currentModeOverridden = false;
    if (this.features.onOff && this.state.onMode !== void 0 && this.state.onMode !== null) {
      const onOffServer = this.agent.get(import_OnOffServer.OnOffServer);
      if (onOffServer !== void 0) {
        if (onOffServer.features.lighting && onOffServer.state.startUpOnOff === import_OnOffCluster.OnOff.StartUpOnOff.On) {
          this.state.currentMode = this.state.onMode;
          currentModeOverridden = true;
        }
        this.reactTo(onOffServer.events.onOff$Changed, this.#handleOnOffDependency);
      } else {
        logger.warn("OnOffServer not found on endpoint, but OnMode is set.");
      }
    }
    if (!currentModeOverridden && this.state.startUpMode !== void 0 && this.state.startUpMode !== null && this.#getBootReason() !== import_GeneralDiagnosticsCluster.GeneralDiagnostics.BootReason.SoftwareUpdateCompleted) {
      this.state.currentMode = this.state.startUpMode;
    }
  }
  changeToMode({ newMode }) {
    this.#assertModeValue("NewMode", newMode);
    this.state.currentMode = newMode;
  }
  #assertCurrentMode() {
    this.#assertModeValue("currentMode", this.state.currentMode);
  }
  #assertStartUpMode() {
    if (this.state.startUpMode !== void 0 && this.state.startUpMode !== null) {
      this.#assertModeValue("startUpMode", this.state.startUpMode);
    }
  }
  #assertOnMode() {
    if (this.state.onMode !== void 0 && this.state.onMode !== null) {
      this.#assertModeValue("onMode", this.state.onMode);
    }
  }
  #assertModeValue(fieldName, mode) {
    if (!this.state.supportedModes.some(({ mode: supportedMode }) => supportedMode === mode)) {
      throw new import_StatusCode.StatusResponseError(
        `Mode ${mode} provided in ${fieldName} is not supported`,
        import_StatusCode.StatusCode.InvalidCommand
      );
    }
  }
  #handleOnOffDependency(newValue, oldValue) {
    if (newValue && !oldValue && this.state.onMode !== void 0 && this.state.onMode !== null) {
      this.state.currentMode = this.state.onMode;
    }
  }
  #getBootReason() {
    const rootEndpoint = this.endpoint.ownerOfType(import_RootEndpoint.RootEndpoint);
    if (rootEndpoint !== void 0 && rootEndpoint.behaviors.has(import_GeneralDiagnosticsBehavior.GeneralDiagnosticsBehavior)) {
      return rootEndpoint.stateOf(import_GeneralDiagnosticsBehavior.GeneralDiagnosticsBehavior).bootReason;
    }
  }
}
class ModeSelectServer extends ModeSelectServerLogic.for((0, import_ClusterType.ClusterType)(import_ModeSelectCluster.ModeSelect.Base)) {
}
//# sourceMappingURL=ModeSelectServer.js.map
